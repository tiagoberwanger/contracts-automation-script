steps:
    - name: 'gcr.io/cloud-builders/gcloud'
      args: ['app', 'deploy']
# Post-deployment script execution
    - name: 'gcr.io/cloud-builders/python:3.11'
      args: ['-m', 'pip', 'install', '-r', 'requirements.txt']
options:
    logging: CLOUD_LOGGING_ONLY
